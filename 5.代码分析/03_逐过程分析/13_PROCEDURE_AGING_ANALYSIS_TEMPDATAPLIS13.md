# PROCEDURE_AGING_ANALYSIS_TEMPDATAPLIS13

- 【代码事实】技术目的：刷新 PLIS13 中间表并抽取 `001013%` 及 `AAA` 范围明细。
- 【合理推断】业务目的：为常规13机构与AAA特殊机构形成统一账龄入口。
- 【合理推断】最小示例：机构维表码AAA且其他过滤满足的借方60行会进入13表；这类行不会因不匹配`001013%`而被排除。

- 【代码事实】用途与接口：阶段 1 PLIS13 抽取，参数 `YYYY-MM`；机构范围是 `001013%` 或精确 `AAA`。证据：过程 `:1-2,:51`，调用脚本 `:15`。
- 【代码事实】流程/对象：START 日志 → TRUNCATE 13 月度/当批表 → 抽取写入 → 完成描述日志 → COMMIT。证据：`:4-59`。
- 【代码事实】粒度/金额/字段：逐总账行；来源 PLIS，金额借方优先或负贷方，业务号回退保单；账户段按科目处理。证据：`:11-50`。
- 【代码事实】机构与过滤：`100001→001`，否则从维表映射；除通用过滤外，机构范围额外含 `AAA`。证据：`:35-55`。
- 【代码事实】事务/日志：清表隐式提交；完成日志状态仍为 START。证据：`:7-8,:57-59`。
- 【合理推断】风险：`AAA` 会绕开常规前缀分区，需防止跨分支重复；并发/失败恢复与其他 PLIS 分支相同。
- 【待业务确认】确认 `AAA` 的业务定义、是否只应归 PLIS13，以及是否可能与 `001013%` 对应记录重叠。
