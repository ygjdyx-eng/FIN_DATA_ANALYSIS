# PROCEDURE_AGING_ANALYSIS_TEMPDATAPLIS07

- 【代码事实】技术目的：刷新 PLIS07 月度/当批表并抽取 `001007%` 机构范围明细。
- 【合理推断】业务目的：形成 PLIS07 账龄处理的当月保单科目明细。
- 【合理推断】最小示例：借方NULL、贷方10写-10；若短名为NULL，现有`NOT LIKE`条件不会选中该行。

- 【代码事实】用途与接口：阶段 1 PLIS07 抽取，入参 `YYYY-MM`；机构范围 `001007%`。证据：过程 `:1-2,:50`，调用脚本 `:9`。
- 【代码事实】顺序/对象：写开始日志，清空 07 月度/当批表，写总账明细，写完成描述日志并提交。证据：`:4-58`。
- 【代码事实】核心规则：来源 PLIS；金额 `NVL(借方,-贷方)`；业务号回退保单；指定科目保留/规范化账户段；机构含 `100001→001` 特例。证据：`:10-49`。
- 【代码事实】过滤：期间为参数，限定四类来源和配置科目，排除两个账簿与特定短名。证据：`:39-54`。
- 【代码事实】事务/日志：2 次 TRUNCATE 隐式提交，末尾显式提交；完成状态标识仍为 START。证据：`:7-8,:56-58`。
- 【合理推断】风险：失败恢复需重跑整个分支；多会话无隔离；未显式携带币种。
- 【待业务确认】确认后续按金额结零时是否允许不同币种共同参与。
