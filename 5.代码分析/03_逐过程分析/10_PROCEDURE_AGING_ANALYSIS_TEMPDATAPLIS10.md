# PROCEDURE_AGING_ANALYSIS_TEMPDATAPLIS10

- 【代码事实】技术目的：刷新 PLIS10 当月中间表并抽取 `001010%` 明细。
- 【合理推断】业务目的：为 PLIS10 分支的累计结零和五档账龄提供输入。
- 【合理推断】最小示例：科目不在保留账户集合时目标账户段为NULL；贷方100写金额-100。

- 【代码事实】用途与接口：阶段 1 PLIS10 抽取，参数 `YYYY-MM`，机构范围 `001010%`。证据：过程 `:1-2,:50`，调用脚本 `:12`。
- 【代码事实】流程：START 日志 → 清空 `temp_month_personal_insurance10`/10 当批表 → 抽取并富化 → 完成描述日志 → 提交。证据：`:4-58`。
- 【代码事实】核心粒度与金额：逐总账行写入，金额 `NVL(ENTERED_DR,-ENTERED_CR)`，来源 PLIS，业务号回退保单号。证据：`:10-49`。
- 【代码事实】映射/过滤：账户段按科目规则，`100001→001`；按期间、来源、科目、机构、账簿和短名过滤。证据：`:24-54`。
- 【代码事实】事务/日志：TRUNCATE 隐式提交；完成日志状态仍是 START。证据：`:7-8,:56-58`。
- 【合理推断】风险：失败时无法回滚清表；依赖维表标量查询单行；无批次键导致并发风险。
- 【待业务确认】验证机构维表唯一键与下游必填字段默认值。
