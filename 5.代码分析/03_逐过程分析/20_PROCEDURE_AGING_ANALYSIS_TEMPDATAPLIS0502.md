# PROCEDURE_AGING_ANALYSIS_TEMPDATAPLIS0502

- 【代码事实】技术目的：维护 PLIS05 历史余额标记并生成五档当批明细。
- 【合理推断】业务目的：排除05分支可结零往来并保留应展示余额。
- 【合理推断】最小示例：月末与凭证日相差2个月进入档1；若同地区科目保单在配置的两个档合计为0，两档明细标记1。

- 【代码事实】用途与接口：阶段 2 PLIS05，参数 `YYYYMM`，处理 05 月度、历史和当批表；月末存于 `VARCHAR2`。证据：`:1,:7,:34,:43,:175`，调用脚本 `:26`。
- 【代码事实】处理链：追加细粒度非零明细 → 2018-10 至 SYSDATE 的月首/月末累计结零 → 五档账龄 → 配置档位组合结零并回写历史 → END。证据：`:13-27,:43-343`。
- 【代码事实】金额粒度：历史追加按多维键+日期 `SUM<>0`；预处理按科目+保单 `SUM=0`；最终分组注释掉地区；跨档按地区+科目+保单 `SUM=0`。证据：`:115-154,:158-171,:261-291,:295-333`。
- 【代码事实】账龄与事务：阈值为 3/12/36/60 月；3 个静态 COMMIT 位置，两个在循环；无同月 DELETE/MERGE。证据：`:230-235,:171,:333,:342`。
- 【代码事实】文本差异：历史回标动态 SQL 外层 `ZERO_CLOSING_MARKER='0'` 未加表别名，Oracle 会解析到当前查询块；核心效果与 06/07 同类。证据：`:326-331`。
- 【代码事实】字段断点：阶段 1 未显式提供本过程读取的多个账簿、期间、产品和人员字段。证据：阶段1 PLIS05 `:11-36`；本过程 `:81-112`。
- 【合理推断】风险：NLS、性能、非幂等、并发和跨维度结零；未限定币种。
- 【待业务确认】验证未限定字段与零余额键的业务定义。
