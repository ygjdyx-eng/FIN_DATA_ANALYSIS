# PROCEDURE_AGING_ANALYSIS_TEMPDATAPLIS02

- 【代码事实】技术目的：刷新 PLIS02 的两张当批表并抽取 `001002%` 范围总账明细。
- 【合理推断】业务目的：形成 PLIS02 机构的账龄计算入口数据。
- 【合理推断】最小示例：借方NULL、贷方80会写金额-80；业务号NULL、保单P2时业务号写P2；`SEGMENT1=100001` 时机构写001。

- 【代码事实】用途与接口：阶段 1 PLIS02 抽取，入参 `YYYY-MM`；机构范围 `001002%`。证据：过程 `:1-2,:45`，调用脚本 `:4`。
- 【代码事实】顺序/对象：START 日志 → TRUNCATE `temp_month_personal_insurance02` 与对应当批账龄表 → 从总账和维表插入 → 完成描述日志 → COMMIT。证据：`:4-54`。
- 【代码事实】粒度与规则：逐总账行写入；来源固定 `PLIS`，金额为 `NVL(借方,-贷方)`，业务号回退保单号，账户段按科目保留/归一化。证据：`:11-44`。
- 【代码事实】机构映射：`SEGMENT1='100001'` 时写 `BRANCH_CODE='001'`，否则由机构维表取前 9 位；源过滤同 PLIS 通用规则。证据：`:34-49`。
- 【代码事实】事务/重跑：TRUNCATE 使本分支当批表可刷新，但形成隐式提交；完成日志状态仍为 `START`。证据：`:7-8,:52-54`。
- 【合理推断】风险：异常时可能留下已清空或部分写入状态；同一共享 schema 并发运行会互相影响；目标 DDL 缺失导致默认值和约束无法确认。
- 【待业务确认】确认 `100001→001` 的特殊机构归属，以及源 `SHORT_NAME IS NULL` 被 `NOT LIKE` 条件排除是否符合口径。
