# PROCEDURE_AGING_ANALYSIS_TEMPDATAPLIS03

- 【代码事实】技术目的：刷新 PLIS03 当月桥接/当批表并抽取 `001003%` 范围的总账明细。
- 【合理推断】业务目的：向 PLIS03 后续账龄和结零步骤提供有符号的保单科目明细。
- 【合理推断】最小示例：借方50、贷方NULL写+50；借方NULL、贷方50写-50，两行若其他业务键一致可在下游净额为0。

- 【代码事实】用途与接口：阶段 1 PLIS03 抽取，入参 `YYYY-MM`；机构范围 `001003%`。证据：过程 `:1-2,:44`，调用脚本 `:5`。
- 【代码事实】流程：记录 START，清空 `temp_month_personal_insurance03` 和对应当批表，抽取总账明细并富化，记录完成描述后提交。证据：`:4-52`。
- 【代码事实】粒度/金额/日期：逐源总账行；会计日期直接取 `DEFAULT_EFFECTIVE_DATE`，金额 `NVL(ENTERED_DR,-ENTERED_CR)`，未见币种列。证据：`:10-43`。
- 【代码事实】映射与过滤：业务号空时回退保单号，账户段按科目处理；`100001→001`，并执行 PLIS 科目、来源、期间、账簿及短名过滤。证据：`:24-48`。
- 【代码事实】事务与监控：2 次 DDL 隐式提交 + 末尾 COMMIT；完成日志状态写成 `START`。证据：`:7-8,:50-52`。
- 【合理推断】风险：若存在多币种，后续未按币种隔离可能净额相抵；标量维表查询依赖单行性；过程不具备并发批次隔离。
- 【待业务确认】补充单币种假设、目标表字段默认值及机构维表唯一性约束。
