# PROCEDURE_AGING_ANALYSIS_TEMPDATAPLIS08

- 【代码事实】技术目的：刷新 PLIS08 两张当批表并抽取 `001008%` 总账明细。
- 【合理推断】业务目的：为 PLIS08 的历史累计和账龄输出准备标准化明细。
- 【合理推断】最小示例：借方70写+70；`SEGMENT1=100001` 时目标机构码写001，业务号空时回退保单。

- 【代码事实】用途与接口：阶段 1 PLIS08 抽取，参数 `YYYY-MM`，机构范围 `001008%`。证据：过程 `:1-2,:50`，调用脚本 `:10`。
- 【代码事实】顺序：START 日志 → TRUNCATE `temp_month_personal_insurance08` 与对应当批表 → 总账/维表抽取 → 完成描述日志 → COMMIT。证据：`:4-58`。
- 【代码事实】粒度/字段：逐总账行；来源固定 PLIS，金额 `NVL(ENTERED_DR,-ENTERED_CR)`，业务号空时取保单号，账户段按科目映射。证据：`:10-49`。
- 【代码事实】机构/过滤：`100001→001`，否则取机构维表前 9 位；执行通用账簿、期间、来源、科目及短名过滤。证据：`:34-54`。
- 【代码事实】事务/日志：TRUNCATE 隐式提交；完成日志的状态仍为 START。证据：`:7-8,:56-58`。
- 【合理推断】风险：非原子、非并发安全；金额表达式依赖借贷字段互斥；目标表未显式写入的后续字段可能为空。
- 【待业务确认】提供表 DDL/触发器并确认借贷字段约束。
